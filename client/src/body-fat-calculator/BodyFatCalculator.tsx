import React, { FunctionComponent } from "react"
import { Grid } from "@mui/material"

export const BodyFatCalcuator:FunctionComponent = props => {
  return <Grid container>
    <Grid>Body Fat Calculator</Grid>
    {/* form */}
    {/* actions */}
    </Grid>
}